Based on the following collection of SQL summaries and comments, please generate a comprehensive classification taxonomy for organizing these SQL queries.

**SQL Summaries and Comments:**
{% for item in sql_items %}
---
**Comment:** {{ item.comment }}
**Summary:** {{ item.summary }}
{% endfor %}

Please analyze all the SQL queries above and generate a comprehensive taxonomy in the following JSON format:

{
    "domains": [
        {
            "name": "domain_name",
            "description": "Brief description of this business domain",
            "examples": ["example1", "example2"]
        }
    ],
    "layer1_categories": [
        {
            "name": "category_name",
            "description": "Brief description of this primary functional category",
            "domain": "associated_domain_name",
            "examples": ["example1", "example2"]
        }
    ],
    "layer2_categories": [
        {
            "name": "subcategory_name",
            "description": "Brief description of this secondary category",
            "layer1": "parent_layer1_category",
            "examples": ["example1", "example2"]
        }
    ],
    "common_tags": [
        {
            "tag": "tag_name",
            "description": "Brief description of when to use this tag",
            "examples": ["example1", "example2"]
        }
    ]
}

Guidelines:
- Create 3-8 business domains based on the data patterns you observe
- For each domain, create 2-5 layer1 categories representing major functional areas
- For each layer1, create 1-4 layer2 subcategories for more specific purposes
- Generate 15-30 common tags that can be applied across different categories
- IMPORTANT: Keep the same language as the input - if most comments/summaries are in Chinese, generate taxonomy in Chinese; if in English, use English
- Focus on business value and functional purpose rather than technical implementation
- Ensure categories are mutually exclusive but tags can overlap
- Make names concise but descriptive (2-4 words max)
- Include realistic examples for each category/tag
- **CRITICAL FORMATTING REQUIREMENT**: ALL domain/layer1/layer2 name fields MUST use underscores instead of spaces
  - Example: "customer_orders" NOT "customer orders"
  - Example: "data_analysis" NOT "data analysis"
  - Example: "user_management" NOT "user management"
  - **VALIDATION**: Every name field must contain NO spaces - only lowercase letters, numbers, and underscores
  - **FINAL CHECK**: Before submitting, verify ALL name fields follow this format
